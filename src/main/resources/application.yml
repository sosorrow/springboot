
spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/world?characterEncoding=utf8&useSSL=true
    username: root
    password: xjt199384

  resources:
    static-locations: ["classpath:/META-INF/resources/", "classpath:/resources/", "classpath:/static/", "classpath:/public/", "classpath:/"]

mybatis:
  mapper-locations: classpath:mapping/*.xml
  type-aliases-package: com.peach.springboot.bean
